apiVersion: constraints.gatekeeper.sh/v1beta1
kind: K8sPropPreventPrivileged
metadata:
  name: prevent-privileged
spec:
  match:
    kinds:
      - apiGroups: [""]
        kinds: ["Pod"]
    namespaces: ["audit-zone"]
  parameters:
    message: "Privileged containers are not allowed"